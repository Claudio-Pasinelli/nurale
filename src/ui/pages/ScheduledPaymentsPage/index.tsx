import { Flex } from '@chakra-ui/react';
import { ScheduledPayments } from '../../organisms';

const ScheduledPaymentsPage = () => {
  return (
    <Flex width='100%'>
      <ScheduledPayments />
    </Flex>
  );
};

export default ScheduledPaymentsPage;

import { Flex } from '@chakra-ui/react';
import { RecuperoPassword } from '../../organisms';

const RecuperoPassworPage = () => {
  return (
    <Flex width='100%'>
      <RecuperoPassword />
    </Flex>
  );
};

export default RecuperoPassworPage;

import { PageLayout } from '../../molecules';

interface Props
{
    name?: string;
}

const ScheduledValues = ({name}:Props) =>
{
    return (
        <PageLayout name={name}>
            altro
        </PageLayout>
    )
}

export default ScheduledValues;
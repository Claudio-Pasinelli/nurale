import { PageLayout } from '../../molecules';

interface Props
{
    name?: string;
}

const Skills = ({name}:Props) =>
{
    return (
        <PageLayout name={name}>
            altro
        </PageLayout>
    )
}

export default Skills;
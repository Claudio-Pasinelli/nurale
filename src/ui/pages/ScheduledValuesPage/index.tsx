import { Flex } from '@chakra-ui/react';
import { ScheduledValues } from '../../organisms';

const ScheduledValuesPage = () => {
  return (
    <Flex width='100%'>
      <ScheduledValues />
    </Flex>
  );
};

export default ScheduledValuesPage;

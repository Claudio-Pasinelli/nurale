import { Flex } from '@chakra-ui/react';
import { Login } from '../../organisms';

const LoginPage = () => {
  return (
    <Flex width='100%'>
      <Login />
    </Flex>
  );
};

export default LoginPage;
